[project]
name = "bilibili-api"
version = "0.2.0"
description = "B站 OAuth Token 管理与 API 自动化（封面/视频/评论）"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
    "qrcode>=7.4",
    "hx-git-db @ git+https://github.com/HengXin666/HX-Git-DB.git",
]
