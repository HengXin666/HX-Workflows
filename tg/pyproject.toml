[project]
name = "hx-tg-workflows"
version = "0.1.0"
description = "Composable Telegram automation tasks for GitHub Actions."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "PyYAML>=6.0.2",
]

[tool.uv]
package = false
