timezone: Asia/Shanghai
tasks:
- id: tg-sign-daily-sign
  enabled: true
  schedule:
  - daily:00:15
  command: uv run python scripts/sign_from_config.py --config config/signins.yml run-enabled
    --mail
  timeout_minutes: 30
